describe("Admin Office pages exist", () => {
  it("calendar page path defined", () => {
    expect("/admin/office/calendar").toBeTruthy();
  });

  it("invoices page path defined", () => {
    expect("/admin/office/invoices").toBeTruthy();
  });
});

  it("calendar page path defined", () => {
    expect("/admin/office/calendar").toBeTruthy();
  });

  it("invoices page path defined", () => {
    expect("/admin/office/invoices").toBeTruthy();
  });
});


















