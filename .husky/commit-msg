#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# --- Enterprise++ Commit-Msg Gate (optional: commitlint) ---
if command -v npx >/dev/null 2>&1; then
  npx --yes commitlint -E HUSKY_GIT_PARAMS 2>/dev/null || true
fi

