#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "📝 Post-Commit: Compliance-Dokumentation..."

# Compliance-Log erstellen (non-blocking)
if [ -f "scripts/log-compliance-status.js" ]; then
  echo "🔍 Compliance-Log für Commit $(git rev-parse --short HEAD)..."
  node scripts/log-compliance-status.js || echo "⚠️ Compliance-Log fehlgeschlagen (nicht-blockierend)"
else
  echo "⚠️ scripts/log-compliance-status.js nicht gefunden"
fi

echo "✅ Post-Commit abgeschlossen" 